{
  "type": "index_parallel",
  "spec": {
    "dataSchema": {
      "dataSource": "fact_events",
      "timestampSpec": { "column": "event_time", "format": "auto" },
      "dimensionsSpec": {
        "dimensions": [
          { "type": "long", "name": "event_id" },
          { "type": "long", "name": "customer_id" },
          { "type": "string", "name": "country" },
          { "type": "string", "name": "event_type" }
        ]
      },
      "metricsSpec": [
        { "type": "doubleSum", "name": "amount", "fieldName": "amount" }
      ],
      "granularitySpec": {
        "type": "uniform",
        "segmentGranularity": "DAY",
        "queryGranularity": "NONE",
        "rollup": false
      }
    },
    "ioConfig": {
      "type": "index_parallel",
      "inputSource": {
        "type": "s3",
        "properties": {
          "endpointConfig": { "url": "minio:9000", "signingRegion": "us-east-1" },
          "clientConfig": { "protocol": "http", "disableChunkedEncoding": true, "enablePathStyleAccess": true },
          "credentialsConfig": { "accessKeyId": "minioadmin", "secretAccessKey": "minioadmin" }
        },
        "prefixes": ["s3://benchmark-data/${SCALE}/fact_events/"]
      },
      "inputFormat": { "type": "parquet" },
      "appendToExisting": false
    },
    "tuningConfig": {
      "type": "index_parallel",
      "maxRowsPerSegment": 5000000,
      "maxNumConcurrentSubTasks": 2,
      "partitionsSpec": { "type": "dynamic" }
    }
  }
}
